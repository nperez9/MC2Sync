# MC2Sync

PS2 Memory card web manager tool. Sync your multiple virtual memory card files into one

## This proyect is possible thanks to the MC2 library By Ross Ridge

I didn't find his github, so I added the file in the repo /pymemcard
